{
  "name": "friend-for-hire-dao",
  "version": "0.1.0",
  "description": "A minimal DAO for hiring fake friends (demo only).",
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat run --network localhost scripts/deploy.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0"
  }
}